## 1. 编辑用户弹窗

- [x] 1.1 新增 `fronted/src/components/UserEditDialog.vue`，接收显示状态与用户 ID，使用 `el-dialog` 展示只读账号及可编辑的姓名、手机号、邮箱。
- [x] 1.2 弹窗打开时重置旧状态并调用 `getUser` 加载详情；增加加载状态与异步结果防串行污染处理，加载失败时关闭弹窗。
- [x] 1.3 复用现有编辑校验与 `updateUser` 调用；更新失败保留输入，提交期间阻止重复提交和意外关闭。
- [x] 1.4 更新成功后按 `effective` 显示“已生效”或“已提交审批”，关闭弹窗并发出 `updated` 事件。

## 2. 用户列表接入

- [x] 2.1 修改 `fronted/src/views/UserListView.vue`，点击行内“编辑”时记录用户 ID并打开编辑弹窗，不再跳转路由。
- [x] 2.2 监听弹窗 `updated` 事件并调用现有 `loadUsers()`，使编辑结果或待审批状态立即反映到列表。
- [x] 2.3 确认新增用户弹窗及其创建后刷新行为不受影响。

## 3. 移除编辑页面

- [x] 3.1 从 `fronted/src/router/index.ts` 移除 `/users/:id/edit` 与 `user-edit` 路由，并同步路由注释。
- [x] 3.2 删除不再使用的 `fronted/src/views/UserFormView.vue`。
- [x] 3.3 全局搜索并清理 `user-edit`、`/users/:id/edit`、`UserFormView` 及路由跳转的失效引用。

## 4. 验证

- [x] 4.1 运行前端类型检查与生产构建，确认 TypeScript 与 Vite 构建通过。（`npm run build` 已通过；仅有现存的大包体积警告。）
- [x] 4.2 浏览器验证编辑弹窗的详情加载、首字段聚焦、取消、Esc、遮罩关闭、重复打开不同用户和基础表单校验。（已用两位现有用户验证，未产生数据写入。）
- [ ] 4.3 验证编辑接口失败时保留输入，详情加载失败时关闭弹窗，提交期间无法重复提交或意外关闭。
- [ ] 4.4 验证审批开关关闭时编辑成功后列表显示最新数据；审批开关开启时提示“已提交审批”且列表显示待审批变更。
- [ ] 4.5 验证新增用户弹窗仍能正常打开、校验和提交。（已验证正常打开且界面未受影响；为避免新增测试数据，未执行提交。）

## 5. 文档同步核对

- [x] 5.1 实现完成后逐项核对 `proposal.md`、`design.md`、`tasks.md` 与代码，代码与已确认设计一致，无需改变方案。
