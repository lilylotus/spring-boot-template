## 1. 依赖与公共约定

- [x] 1.1 在 `build.gradle` 中增加并锁定 Bouncy Castle Provider 依赖，运行 `gradlew.bat dependencies` 验证依赖可解析且无意外冲突
- [x] 1.2 建立工具类共享的 Base64、UTF-8、必填参数和异常转换约定，并通过非法 Base64、空值及空白值测试验证一致行为

## 2. 国际密码算法工具

- [x] 2.1 实现 `RsaUtils` 的 2048 位密钥生成、X.509/PKCS#8 Base64 编解码及 OAEP-SHA-256 分段加解密，通过长文本往返、错误私钥和密钥格式测试验证
- [x] 2.2 实现 `RsaUtils` 的 SHA-256 with RSA 签名与验签，通过正常签名、内容篡改、错误公钥和非法签名测试验证
- [x] 2.3 实现 `AesUtils` 的 256 位密钥生成和版本化 AES-GCM 密文封装，通过文本往返、随机 IV、密文篡改、错误密钥及格式版本测试验证

## 3. 国密算法工具

- [x] 3.1 实现 `Sm2Utils` 的 `sm2p256v1` 密钥生成、X.509/PKCS#8 Base64 编解码和 C1C3C2 加解密，通过文本往返、错误私钥及密钥格式测试验证
- [x] 3.2 实现 `Sm2Utils` 的 SM3 with SM2 DER 签名与验签，通过正常签名、内容篡改、错误公钥和非法签名测试验证
- [x] 3.3 实现 `Sm4Utils` 的 128 位密钥生成和版本化 SM4-GCM 密文封装，通过文本往返、随机 IV、密文篡改、错误密钥及格式版本测试验证

## 4. 摘要与整体验证

- [x] 4.1 实现 `DigestUtils` 的 SHA-256 与 SM3 Base64 摘要，通过公开标准测试向量及重复计算测试验证
- [x] 4.2 为五个工具类补充中文 Javadoc，核对公共 API、算法参数、密钥格式和异常说明与设计文档一致，并运行 Javadoc 任务验证无新增警告
- [x] 4.3 运行 `gradlew.bat test` 和 `gradlew.bat build`，验证全部新增及既有测试通过且工作区不产生应提交的构建文件
