## 1. 新增用户弹窗

- [x] 1.1 新增 `fronted/src/components/UserCreateDialog.vue`，使用 `el-dialog` 承载账号、姓名、手机号、邮箱表单，并复用现有新增校验与 `createUser` 接口调用逻辑。
- [x] 1.2 完成弹窗生命周期处理：打开时展示干净表单并聚焦首个字段；失败时保留输入；关闭后清空数据与校验；提交中阻止重复提交和意外关闭。
- [x] 1.3 提交成功后按 `effective` 显示“已生效”或“已提交审批”，关闭弹窗并向父组件发出 `created` 事件。

## 2. 用户列表接入

- [x] 2.1 修改 `fronted/src/views/UserListView.vue`，将“新增用户”按钮由路由跳转改为打开新增用户弹窗。
- [x] 2.2 监听弹窗 `created` 事件并调用现有 `loadUsers()`，确保创建完成后列表立即展示最新用户和审批状态。

## 3. 路由与编辑页面收敛

- [x] 3.1 从 `fronted/src/router/index.ts` 移除 `/users/new` 与 `user-new` 路由。
- [x] 3.2 将 `fronted/src/views/UserFormView.vue` 收敛为仅处理编辑模式，移除新增模式分支，确认 `/users/:id/edit` 行为不变。
- [x] 3.3 全局搜索并清理 `user-new`、`/users/new` 及新增模式的失效引用和注释。

## 4. 验证

- [x] 4.1 运行前端类型检查与生产构建，确认 TypeScript 与 Vite 构建通过。（`npm run build` 已通过；仅有现存的大包体积警告。）
- [ ] 4.2 验证取消、Esc、遮罩关闭、重复打开、校验失败和接口失败时的表单状态符合设计。（已在浏览器验证取消、Esc、重复打开、首字段聚焦和必填校验；接口失败与提交期间关闭保护尚未通过可控失败场景实测。）
- [ ] 4.3 验证审批开关关闭时新增成功提示“已生效”并刷新列表；审批开关开启时提示“已提交审批”并在列表显示“待审批”。
- [ ] 4.4 验证编辑用户入口仍跳转 `/users/:id/edit`，详情加载与编辑提交不受影响。（已验证路由跳转和用户详情加载；为避免修改现有管理员数据，未执行编辑提交。）

## 5. 文档同步核对

- [x] 5.1 实现完成后逐项核对 `proposal.md`、`design.md`、`tasks.md` 与代码，代码与已确认设计一致，无需改变方案。
