## 1. 公共契约与配置

- [x] 1.1 扩展锁服务接口，提供仅传业务锁键的常用获取入口及仅传锁键和回调的同步入口，保留显式时长高级入口；通过编译与契约测试验证默认调用不要求时长参数且令牌不会由公开接口或字符串表示泄露
- [x] 1.2 扩展锁配置，提供默认等待 3 秒、默认租期 30 秒、默认续期 10 秒，并复用已有字符串模板与连接工厂；通过最小上下文测试验证默认值、环境覆盖、非法时长和依赖注入，确认不改变现有 JSON 模板

## 2. 获取与安全释放

- [x] 2.1 调整公共入口参数解析，使默认调用从配置取得等待时间与租期、显式调用保持原语义，并继续实现原子带租期获取；通过单元测试验证默认值和显式覆盖均传入正确 Redis 参数
- [x] 2.2 实现单调时钟、有界休眠与有限重试，处理零等待、截止时间、迟到成功、租期耗尽及线程中断；通过可控时钟和休眠测试验证次数、预算、已获锁清理与异常传播
- [x] 2.3 实现原子比较令牌并删除的释放脚本；通过测试验证合法句柄释放、重复释放、错误或过期令牌不能删除当前锁，并确认仅涉及目标锁键
- [x] 2.4 明确传播 Redis 获取及释放错误；通过故障注入测试确认不降级、不吞异常、不继续执行回调或无令牌删除

## 3. 自动续期与所有权状态

- [x] 3.1 调整受 Spring 管理的专用锁续期调度器和句柄状态机，使默认调用使用配置的 10 秒续期间隔；通过单元测试验证默认、配置覆盖和显式时长下注册一次任务、释放或应用关闭时取消任务，并确认线程命名和日志不泄露令牌
- [x] 3.2 实现原子比较令牌续期脚本；通过测试验证匹配令牌恢复原租期、键不存在或令牌不匹配不能续期，并确认续期与释放并发时不会延长其他持有者的锁
- [x] 3.3 实现续期 Redis 故障、调度拒绝和所有权不匹配的失锁处理；通过测试验证停止续期、句柄可观察为失锁、不重建锁，且成功业务不会被报告为正常成功

## 4. 同步业务回调

- [x] 4.1 扩展有返回值与无返回值回调入口，提供仅传锁键和回调的默认调用；通过测试验证竞争失败不执行回调、成功仅执行一次、正常和空结果原样返回、最终释放，以及回调期间续期失锁在结束时失败反馈
- [x] 4.2 实现业务异常优先与附加清理异常机制；通过测试覆盖业务成功但失锁、释放故障、续期故障、业务异常或错误与清理失败同时发生的情况，确保不自动重试业务

## 5. 集成验证与接入说明

- [ ] 5.1 新增显式启用的最小 Redis 集成测试，使用两个独立客户端和随机测试前缀；在独立测试 Redis 上验证同键竞争、不同键独立、释放接手、自动续期、租期过期和旧持有者误删防护，确认清理只针对本测试键且未加载 MySQL 或 Nacos
- [x] 5.2 更新中文接入文档，优先展示仅传锁键及回调的默认调用，说明默认等待 3 秒、租期 30 秒、续期 10 秒和按环境配置覆盖方式；逐项审查租期、续期、无可重入和故障切换风险说明与实现一致
- [x] 5.3 使用 Gradle Wrapper 运行锁模块单元测试及编译；记录测试数量和结果，真实 Redis 测试未启用或环境不可用时如实标注，不能把跳过算作通过
