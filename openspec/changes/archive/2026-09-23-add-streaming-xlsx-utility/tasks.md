# Tasks

## 1. 公共契约与配置

- [x] 1.1 新增 `ExcelColumn`、`ExcelReadOptions`、`ExcelWriteOptions`、`ExcelRowContext`、`ExcelRowHandler` 和错误类型，使用中文 Javadoc 描述字段含义、默认值、单位、限制及流所有权，并通过配置默认值和非法边界单元测试验证契约。
- [x] 1.2 新增静态门面 `ExcelUtils` 的对象/Map 列表读取、逐行读取和惰性导出方法签名，验证项目编译且公共方法均有参数、返回值和异常 Javadoc。

## 2. 有界 XLSX 读取基础设施

- [x] 2.1 实现输入流限量复制、OOXML/XLSX 格式校验、只读 OPC 包打开和调用级临时目录生命周期，测试合法 XLSX、XLS、伪装内容、损坏内容、输入超限及流不被关闭。
- [x] 2.2 实现磁盘支持的共享字符串表、定长偏移索引、小型 LRU 和临时字节预算，测试富文本拼接、转义字符、重复字符串、高基数字符串、预算超限和失败清理。
- [x] 2.3 实现安全 SAX 工作表读取器、样式/缓存公式值解析、标题与数据区域选择、空行策略及累计行列单元格限制，测试默认/自定义区域、缺失工作表、稀疏单元格、公式缓存和各资源边界。

## 3. 对象和 Map 导入

- [x] 3.1 实现对象列元数据缓存、父类字段扫描、标题匹配、无参构造和字段赋值，测试排序、未知列、缺失/重复标题、重复顺序、必填字段和不支持类型。
- [x] 3.2 实现字符串、数字、布尔、枚举和日期时间转换，验证范围检查、整数精度、空值、格式错误及异常中的一基行列、标题、字段和截断值摘要。
- [x] 3.3 实现对象与 Map 的同步逐行导入及提前停止，并以此封装有界列表接口，测试行顺序、Map 列顺序、缺失单元格、回调异常 cause、列表超限提示和调用方输入流所有权。

## 4. 对象和 Map 流式导出

- [x] 4.1 实现基于 SXSSF 的惰性写出、固定行窗口、压缩临时文件、有限样式复用、工作表名称清理和自动分表，测试跨表重复标题、最大工作表限制及成功/失败清理。
- [x] 4.2 实现对象导出列映射与安全单元格写入，测试文本、公式样式文本、布尔、日期、普通数字以及超出 Excel 精度的整数和十进制值。
- [x] 4.3 实现 `LinkedHashMap` 显式列和首行推断导出，测试稳定列序、空数据标题、缺失键、未知键、空 Map 及输出流在成功和失败时均不被关闭。

## 5. 生产验证与文档

- [x] 5.1 增加端到端往返测试，覆盖对象和 Map 的导入导出、非 ASCII 标题、空值、日期、多工作表与损坏输入，并运行 `gradlew.bat test` 验证全部测试通过。
- [x] 5.2 增加独立 JVM 受限堆测试，以惰性数据源和大量唯一字符串分别验证流式导入导出的峰值内存不随总行数线性增长，并验证临时文件在成功、提前停止、异常和限制失败后清理。
- [x] 5.3 在公共类 Javadoc 中补充上传流调用、对象映射、Map 映射、小数据列表、大数据逐行/惰性导出、并发限制及原子文件写出的示例，并运行 `gradlew.bat javadoc` 验证无新增文档警告。
- [x] 5.4 运行 `gradlew.bat clean build` 和 `openspec validate add-streaming-xlsx-utility --strict`，确认编译、测试、Javadoc 与 OpenSpec 严格校验全部通过且未提交 `build/` 或临时 XLSX 文件。
- [x] 5.5 审查 `org.example.simple.util.excel` 包内全部源码，为类、字段和方法补充便于维护的中文 Javadoc 或必要行内注释，
  重点说明职责、默认值、单位、状态语义、资源边界、异常行为和复杂实现原因，并运行 Javadoc 与测试验证不改变运行时行为。
- [x] 5.6 将普通文本单元格的默认读取行为调整为去除前后空白，保留显式关闭修剪的配置能力，并补充默认值、对象导入、
  Map 导入及关闭修剪场景的单元测试，运行全量测试和 OpenSpec 严格校验。
- [x] 5.7 在对象字段转换阶段使用字符串空白语义统一判空，使必填字段和基本类型拒绝纯空白文本、可空引用字段映射为
  `null`，并补充默认修剪关闭时的必填、可空及 Unicode 空白测试，运行全量测试和 OpenSpec 严格校验。
