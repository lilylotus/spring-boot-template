## MODIFIED Requirements

### Requirement: 指定用户多级审批链配置

系统 SHALL 允许管理员针对“用户新增”“用户编辑”分别维护一个按 `bizType + scope` 映射的流程模板；模板草稿由一个或多个指定审批人的审批节点组成，成功发布后形成不可变版本并作为该业务后续审批的当前生效流程。

#### Scenario: 配置多级审批链

- **WHEN** 管理员绘制完整单路径流程、保存草稿并成功发布
- **THEN** 系统生成递增模板版本并按连线路径保存审批人顺序，之后的新审批使用该版本

#### Scenario: 审批链未配置时开启审批开关

- **WHEN** 管理员开启全局审批开关，但某业务类型没有当前生效模板版本
- **THEN** 系统拒绝开启并提示先发布该业务流程

#### Scenario: 修改审批链不影响进行中的审批

- **WHEN** 某模板存在运行中实例时管理员保存并发布新版本
- **THEN** 运行中实例仍按原流程定义和审批人快照推进，新版本仅用于后续实例

### Requirement: 审批任务的发起与顺序流转

系统 SHALL 在业务请求需要审批时按 `bizType + scope` 解析当前生效模板，使用该版本的流程定义启动实例，并按模板审批节点顺序逐级创建任务；业务代码不得指定 Flowable 流程定义标识。

#### Scenario: 发起审批

- **WHEN** 审批开关开启且业务方以 `bizType`、`bizId` 和业务上下文发起用户新增/编辑审批
- **THEN** 审批域解析该业务当前生效模板版本，创建第一级任务，并记录业务、模板版本、流程定义与实例关联

#### Scenario: 逐级流转

- **WHEN** 当前级别审批人同意且该级别不是最后一级
- **THEN** 系统按该实例绑定版本的审批顺序创建下一级任务

#### Scenario: 最后一级同意后整体通过

- **WHEN** 最后一级审批人同意
- **THEN** 系统判定业务请求整体审批通过

#### Scenario: 任一级别驳回则整体驳回

- **WHEN** 任一级审批人驳回
- **THEN** 系统立即判定整体不通过且不再创建后续任务

### Requirement: 业务数据与审批运行时数据的间接关联

系统 SHALL 通过审批域维护的关联记录保存业务键、模板版本、流程定义和流程实例之间的关系；业务模块自身的数据结构不直接持有 Flowable 运行时标识。

#### Scenario: 建立关联

- **WHEN** 审批域按生效模板为业务请求启动流程
- **THEN** 系统保存 `bizType + bizId`、复合 businessKey、模板版本、流程定义 ID 和流程实例 ID 的关联

#### Scenario: 依据业务单据反查审批状态

- **WHEN** 业务模块凭 `bizType + bizId` 查询审批
- **THEN** 审批域通过关联记录返回对应实例、模板版本和审批状态，不要求业务读取 Flowable 表

#### Scenario: 依据流程实例反查业务

- **WHEN** 系统持有流程实例 ID 或从实例读取 businessKey
- **THEN** 审批域可还原原始业务类型和业务 ID，并定位业务审批关联
