## 1. 连接池配置模型

- [x] 1.1 新增不可变 `HttpConnectionPoolConfig`、生产推荐默认值和构建器，通过单元测试验证 200/50 容量、5 分钟 TTL、5 秒空闲校验及 30 秒空闲回收默认值
- [x] 1.2 实现连接池容量和时长的集中校验，通过单元测试验证非正容量、单路由容量超过总量、空时长和非法时长均被拒绝
- [x] 1.3 将连接池配置接入 `HttpClientConfig` 默认值、构建器和兼容四参数构造器，将 `requestTimeout` 更名为 `responseTimeout`，并通过测试验证默认连接与响应超时均为 3 秒

## 2. 生产连接池实现

- [x] 2.1 将容量、`STRICT` 并发策略、`LIFO` 复用策略、TTL、空闲校验和锁外释放映射到 `PoolingHttpClientConnectionManagerBuilder`，通过配置映射测试验证全部选项生效
- [x] 2.2 在 Apache 客户端启用过期连接和可配置空闲连接回收，验证全局重配置和 `shutdown()` 会关闭旧客户端及其回收资源
- [x] 2.3 重构 `createRequestConfig` 以独立接收连接和响应超时，分别映射连接建立、连接池获取、响应等待和套接字读取阶段，通过不同超时值的慢服务和连接池竞争测试验证分离生效

## 3. 连接池行为验证

- [x] 3.1 使用单路由容量为 1 的阻塞本地服务验证严格容量上限，并验证第二个请求按连接超时失败
- [x] 3.2 使用本地服务端连接端口验证有效连接复用，并使用短 TTL 与空闲回收配置验证过期或长时间空闲连接不再复用

## 4. 质量与一致性

- [x] 4.1 更新超时配置入口的中文 Javadoc，检查英文标识符、四空格缩进、无通配符 import 及 120 字符行宽
- [x] 4.2 运行 HTTP 定向测试、全部测试、构建及严格 OpenSpec 校验，确认实现与文档一致
