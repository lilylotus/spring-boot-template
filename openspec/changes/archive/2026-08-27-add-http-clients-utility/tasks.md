## 1. 公共模型与配置

- [x] 1.1 新增 HTTP 错误分类、统一客户端异常和不可变原始响应模型，并通过单元测试验证状态码、响应头、响应字节及底层原因均可保留
- [x] 1.2 将不可变客户端配置接入静态全局 `configure` API，校验默认超时和跳过 SSL 选项，并通过单元测试验证合法替换、非法配置不影响原状态及关闭后重新配置
- [x] 1.3 调整不可变请求、查询参数、请求头和请求级超时构建 API，使公开入口接收 HTTP/HTTPS URL 文本，并通过单元测试验证 URL 文本、GET 请求体限制和非法参数校验
- [x] 1.4 新增 JSON、URL 编码表单、multipart 和文件二进制请求体模型，并通过单元测试验证字段约束、文件可读性和媒体类型约束
- [x] 1.5 将客户端配置拆分为连接超时和请求超时，默认均设为 5 秒，并通过单元测试验证默认值、构建器覆盖和非法值校验

## 2. HTTP 客户端核心实现

- [x] 2.1 将 `HttpClients` 重构为不可实例化的静态门面，使用读写锁安全管理共享 Apache 客户端，并通过生命周期和并发测试验证连接复用、原子重配置、幂等关闭及关闭后拒绝请求
- [x] 2.2 将 `get`、`post`、`put`、`delete`、`execute` 和 `executeJson` 改为静态入口，在内部完成 URL 解析、请求头和查询参数编码，并通过本地 HTTP 服务集成测试验证无需实例即可完成全部调用
- [x] 2.3 实现 URL 编码表单、JSON、multipart 文本与多文件、二进制文件实体构造，并通过本地 HTTP 服务集成测试逐字节验证内容类型、UTF-8 文本、boundary、文件名和文件内容
- [x] 2.4 实现响应完整读取、`2xx` 成功判定和非 `2xx` 异常映射，并通过集成测试验证正常、空响应体、错误状态及响应资源释放行为
- [x] 2.5 实现基于 `Class<T>` 和 `TypeReference<T>` 的 JSON 响应转换，并通过测试验证普通对象、泛型集合、非法 JSON 和类型不兼容场景

## 3. SSL 与超时

- [x] 3.1 将默认 JVM 信任链和主机名校验接入静态共享客户端，并通过自签名本地 HTTPS 服务测试验证默认静态调用拒绝不可信证书
- [x] 3.2 将显式跳过证书链与主机名校验接入静态全局配置，并通过同一自签名 HTTPS 服务测试验证重配置后的静态调用可成功连接
- [x] 3.3 将静态全局默认超时映射到连接池获取、连接建立和响应等待阶段，并通过可控延迟服务测试验证重配置前后的超时行为
- [x] 3.4 保留单次请求超时覆盖且不改变静态全局默认值，并通过连续静态请求测试验证覆盖优先级、请求间隔离和非法覆盖值校验
- [x] 3.5 将连接超时仅映射到连接建立阶段、请求超时映射到连接池获取和响应等待阶段，并验证单次请求只覆盖请求超时

## 4. 质量与文档

- [x] 4.1 更新静态公共 API、全局配置和关闭行为的中文 Javadoc 与调用示例，检查全部自然语言、英文标识符、四空格缩进及 120 字符行宽符合仓库规范
- [x] 4.2 核对 multipart 所需类的依赖来源，仅在编译缺失时补充最小依赖，并使用 `gradlew.bat dependencies` 验证无不必要的版本冲突
- [x] 4.3 运行 `gradlew.bat test` 和 `gradlew.bat build`，验证静态调用测试与现有 HTTP、RPC、日志测试全部通过且无新增弃用警告
- [x] 4.4 运行 `openspec validate add-http-clients-utility --strict` 并复核实现、测试、`proposal.md`、`design.md`、规格和任务状态保持一致
- [x] 4.5 初始化默认 `ObjectMapper`，通过测试验证忽略未知字段、序列化省略 `null`、Java 时间 ISO-8601 处理及自定义映射器不被修改
- [x] 4.6 运行完整测试、构建和 OpenSpec 严格校验，复核实现与本次补充设计保持一致
