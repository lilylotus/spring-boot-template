## 1. 后端开启校验

- [x] 1.1 在 `approval.config` 内集中定义当前受全局开关控制的业务类型 `USER_CREATE`、`USER_EDIT`，不引入对 `identity` 实现包的依赖。
- [x] 1.2 增强 `ApprovalConfigServiceImpl.updateApprovalSwitch`：开启前校验两条审批链均至少配置一级，缺失时返回明确业务错误且不修改开关；关闭逻辑保持兼容。
- [x] 1.3 扩展 Service 测试，覆盖配置完整时开启、任一链缺失时开启失败、关闭不受配置影响。
- [x] 1.4 扩展 Controller 测试，验证开关更新成功及配置不完整时的统一错误响应。

## 2. 前端配置类型与 API

- [x] 2.1 新增 `src/types/approval-config.ts`，定义开关、业务类型、审批级别及保存请求类型，与后端 DTO 保持一致。
- [x] 2.2 新增 `src/api/approval-config.ts`，封装开关查询/更新和两条审批链查询/保存接口。

## 3. 审批配置页面

- [x] 3.1 新增 `ApprovalSettingsView.vue`，并行加载开关、用户列表、用户新增审批链和用户编辑审批链，提供明确的加载、失败与空状态。
- [x] 3.2 为两种业务动作分别实现审批轨道，支持增加、删除、上移、下移级别，按当前顺序自动生成连续 `levelNo`。
- [x] 3.3 审批人使用“姓名（账号）”用户选择器，只允许新选 `ACTIVE` 用户；旧失效审批人保留展示并阻止未修正配置保存。
- [x] 3.4 两条审批链分别保存并维护独立的脏状态、提交状态；失败时保留编辑内容，成功后重新读取服务端配置。
- [x] 3.5 实现全局开关交互：开启前校验两条链完整且无未保存修改；关闭前二次确认；请求失败时恢复原值。

## 4. 路由与导航

- [x] 4.1 新增 `/approval-settings` 路由，并更新路由职责说明。
- [x] 4.2 在 `App.vue` 顶部导航新增“审批配置”，保持当前操作人选择器和窄屏布局可用。

## 5. 验证

- [x] 5.1 运行后端测试与完整构建，确认审批配置、用户新增/编辑和审批中心没有回归。
- [x] 5.2 运行前端类型检查与生产构建，确认通过。
- [ ] 5.3 浏览器验证已有配置回显、两条审批链编辑和保存、有效用户选择、失效审批人提示、开关开启/关闭及失败回滚。
- [ ] 5.4 浏览器验证桌面和窄屏布局、键盘焦点与操作反馈。

## 6. 文档同步核对

- [x] 6.1 实现完成后逐项核对 `proposal.md`、`design.md`、`tasks.md` 与代码；若核心设计必须变化，停止编码、更新文档并重新等待确认。
