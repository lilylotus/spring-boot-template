## 1. 前端列表展示

- [x] 1.1 在 `ApprovalCenterView.vue` 增加用户业务标识到 `realName` 的统一映射函数，并为无法映射的情况保留 `bizId` 回退
- [x] 1.2 将“待我审批”和“审批记录”的“业务标识”列改为“审批用户”，共用姓名显示函数

## 2. 验证与文档核对

- [x] 2.1 运行 `npm run build`，确认 TypeScript 检查与 Vite 构建通过
- [ ] 2.2 手工验证用户新增、用户编辑在两个页签显示姓名，并验证未知或无法匹配的数据仍显示原 ID
- [x] 2.3 对照 proposal、design、tasks 与 spec 核对最终实现，确认未改变后端接口和业务关联键
