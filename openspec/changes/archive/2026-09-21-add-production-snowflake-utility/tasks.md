## 1. 雪花生成器实现

- [x] 1.1 新增不可实例化的 `SnowflakeUtils` 及包可见内部生成器，定义固定纪元和 41/5/5/12 位布局，并通过位解析测试验证时间戳、数据中心、工作节点和序列字段
- [x] 1.2 实现 `initialize(int, int)` 的参数范围校验、相同配置幂等和冲突配置拒绝，并通过初始化状态与边界测试验证
- [x] 1.3 实现线程安全的 `nextId()`、毫秒内序列递增和序列耗尽等待，通过连续递增、4096 序列边界与正数结果测试验证
- [x] 1.4 实现 5 毫秒以内时钟回拨等待、严重回拨失败、时间范围校验和中断处理，并使用可控时钟测试验证所有异常路径
- [x] 1.5 为类、常量、状态字段和公共方法补充中文 Javadoc，说明节点唯一性责任、容量、生命周期及错误行为，并运行 Javadoc 任务验证

## 2. 并发与构建验证

- [x] 2.1 新增 `SnowflakeUtilsTest`，使用多线程批量生成 ID，验证无重复、节点位正确且每个 ID 为正数，并运行专项测试
- [x] 2.2 运行 `gradlew.bat test` 和 `gradlew.bat build`，验证新增功能与既有测试全部通过

## 3. 默认初始化调整

- [x] 3.1 实现首次 `nextId()` 自动使用 `(0, 0)` 初始化，与显式初始化共用同步保护，并验证显式配置优先完成时不被默认配置覆盖
- [x] 3.2 更新中文 Javadoc，说明默认节点、首次配置固定及多实例显式分配要求，并运行 Javadoc 校验
- [x] 3.3 更新隔离静态生命周期测试，覆盖直接发号、并发首次发号、相同配置幂等、冲突配置拒绝及两种初始化竞争，并通过专项测试
- [x] 3.4 运行全量测试和构建，确认原有时钟、序列及并发唯一性行为不受影响
