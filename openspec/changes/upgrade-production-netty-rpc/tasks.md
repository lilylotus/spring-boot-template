## 1. 审阅与基础准备

- [x] 1.1 获得用户对 proposal、design、specs 和 tasks 的明确确认后再开始代码变更。
- [ ] 1.2 确认 Java 21，使用 Gradle 包装器验证现有测试基线及 Nacos、Micrometer、OpenTelemetry 依赖兼容性，锁定依赖版本。
- [x] 1.3 新增经过边界校验的客户端、服务端和调用配置，覆盖超时、容量、心跳、停机和重试参数。
- [x] 1.4 按设计默认值显式配置服务端 option/childOption 及客户端 option，应用固定默认线程数及手动线程配置、池化分配器、TCP 参数和缓冲水位。
- [ ] 1.5 验证默认配置和覆盖配置在真实 Channel 及执行器上生效，覆盖非法组合、选项失败和配置溢出；验证不同可见 CPU 核数下默认线程数相同，拒绝线程数 0/负值，禁止使用 Netty 自动线程数构造入口。

## 2. 协议与序列化

- [x] 2.1 定义十九字节头、消息类型和 long requestId，将请求参数及响应结果改为带空值标记的字节载荷，移除 JSON 专用模型依赖。
- [x] 2.2 实现长度拆帧与头部校验，限制帧大小、序列化编号和控制消息格式。
- [ ] 2.3 将较重序列化转换移至有界执行器，验证 ByteBuf 引用释放及拒绝路径。
- [x] 2.4 使用 EmbeddedChannel 覆盖每个头部切分点、消息体拆分、多帧合并、非法帧和长度边界。

- [x] 2.5 实现编号注册表、JSON/Protostuff 两种实现、客户端默认及调用级选择，确保响应沿用请求编码且校验匹配。
- [ ] 2.6 实现固定字段编号信封、受控类型 Schema、泛型 Type 入口和日期时间适配，验证 Java 21、空值、基础类型、DTO、集合和不支持类型拒绝。
- [ ] 2.7 覆盖双编码并发、同连接混合编码、未知/禁用编号、响应编号不匹配、畸形载荷、嵌套深度及集合上限、字段新增兼容与 LinkedBuffer 清理。
- [ ] 2.8 将同步/异步及双实例环回测试参数化为 JSON 和 Protostuff 两组，交付编码配置示例、支持类型与字段演进说明。

## 3. 异步与超时核心

- [x] 3.1 实现客户端共享 PendingRequest 表、单调 requestId、生命周期准入和条件删除终态路径。
- [ ] 3.2 实现共享 HashedWheelTimer、分级预算、句柄安装竞态及取消传播。
- [ ] 3.3 实现有界完成执行器及预留容量，保证用户回调不在 I/O 或计时线程执行。
- [x] 3.4 提供 invokeAsync 并让同步 invoke 委托相同机制，保留直连入口，处理同步中断与线程限制。
- [ ] 3.5 使用屏障和可控调度覆盖乱序、跨 Channel 伪响应、重复响应、超时竞争、取消、写失败、关闭及 requestId 耗尽。

## 4. 服务发现与连接

- [x] 4.1 定义发现与注册接口、不可变实例快照和可控测试发现实现。
- [ ] 4.2 接入 Nacos 注册/注销及订阅/取消订阅，支持 namespace、group、服务版本和外部凭证配置。
- [ ] 4.3 实现异步连接、实例代次检查、移除排空和每实例单飞重连。
- [ ] 4.4 实现发现缓存有效期、空快照和订阅失败语义，并测试迟到连接回调与局部断连。

## 5. 负载均衡

- [x] 5.1 定义 LoadBalancer 和候选过滤规则，实现线程安全轮询并测试空集合及计数边界。
- [x] 5.2 实现平滑加权轮询并验证权重比例、非正权重过滤及动态权重变化。
- [x] 5.3 实现一致性哈希与显式路由键，验证稳定映射及节点变更时仅部分键迁移。

## 6. 服务端与生产治理

- [ ] 6.1 保持业务线程隔离，实现并发准入、服务速率限制、队列过期拒绝及客户端在途上限。
- [ ] 6.2 配置连接上限及写缓冲水位，实现不可写时的有界处理，测试慢消费者和过载。
- [ ] 6.8 实现每 Channel 待写字节硬上限与端级报文预算，覆盖半包累积、编解码队列、控制消息和所有失败释放路径，验证水位本身不作为硬上限。
- [ ] 6.3 实现独立心跳关联与空闲失活检测，验证业务池饱和时心跳仍可推进。
- [ ] 6.4 实现默认关闭的幂等有限重试，验证总预算、退避取消及禁止重试类别。
- [ ] 6.5 实现客户端和服务端 closeAsync、排空期限、幂等关闭及启动失败回滚。
- [ ] 6.6 接入 TLS/mTLS 与主机名校验、握手超时，验证不可信证书及服务签名白名单。
- [ ] 6.7 接入 Micrometer 指标和 OpenTelemetry 上下文传播，测试终态计数、线程切换及敏感信息保护。

## 7. 集成验收与交付

- [ ] 7.1 添加双实例环回集成测试，覆盖同步/异步、节点变化、断连、心跳、限流和停机排空。
- [ ] 7.2 提供并执行独立 Nacos 集成验证入口；环境不可用时明确记录未验证及所需配置。
- [ ] 7.3 编写并执行可复现压力与故障验证，记录吞吐、p95/p99、错误率、队列上限及资源回收情况。
- [ ] 7.4 更新中文使用文档与同步/异步示例，说明 Nacos、负载均衡、TLS、监控、重试语义及旧协议迁移。
- [ ] 7.5 执行 gradlew.bat test、gradlew.bat build 和 OpenSpec 严格校验，核对代码、测试与文档一致，报告未验证边界。
- [ ] 7.6 交付生产默认参数表、覆盖方式、脱敏生效配置和堆/直接内存预算说明，以慢发包、慢消费者、最大帧并发和泄漏检测验证资源边界。
