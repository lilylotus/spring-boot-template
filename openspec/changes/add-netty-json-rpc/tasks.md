## 1. 公共协议与序列化

- [x] 1.1 在 `org.example.simple.rpc.common` 中建立请求、响应、错误类型和 RPC 异常模型，并校验必要字段
- [x] 1.2 定义消息序列化接口，实现复用 `ObjectMapper` 且不启用默认多态类型的 Jackson JSON 序列化器
- [x] 1.3 实现四字节长度前缀的请求与响应编解码器，并支持可配置的消息长度上限
- [x] 1.4 编写序列化往返、拆包、粘包、畸形 JSON 和超长消息测试

## 2. 服务端

- [x] 2.1 在 `org.example.simple.rpc.server` 中实现显式服务注册表，仅缓存注册服务接口的公开方法签名
- [x] 2.2 实现请求分发、参数类型匹配、JSON 参数转换、反射调用及安全的失败响应映射
- [x] 2.3 实现 Netty 服务端启动、管线配置、有界业务执行器以及幂等关闭生命周期
- [x] 2.4 编写已注册服务、未知服务、未知方法、参数不匹配、业务异常和执行器拒绝测试

## 3. 客户端

- [x] 3.1 在 `org.example.simple.rpc.client` 中实现请求标识生成、待完成调用表及响应关联处理
- [x] 3.2 实现客户端连接、同步 `invoke` 调用、返回值转换、超时清理和远端错误映射
- [x] 3.3 处理写入失败、连接关闭、事件循环线程误用及幂等关闭，并结束所有未完成调用
- [x] 3.4 编写成功响应、失败响应、并发乱序响应、调用超时、断开连接和重复关闭测试

## 4. 集成验证与文档同步

- [x] 4.1 使用本机环回地址编写客户端到服务端的 JUnit 5 集成测试，覆盖成功调用、重载方法和业务异常
- [x] 4.2 运行 `./gradlew test` 和 `./gradlew build`，确认无测试失败或新增编译警告
- [x] 4.3 核对实现、测试、`proposal.md`、`design.md` 和增量规格保持一致，并同步记录实施期间的设计变化

## 5. Jackson 配置与命名规范

- [x] 5.1 配置 `JacksonJsonSerializer` 忽略未知字段，并让 `Date` 与 `LocalDateTime` 使用 `yyyy-MM-dd HH:mm:ss`
- [x] 5.2 补充未知字段、日期时间序列化往返及非法日期时间格式测试
- [x] 5.3 将 RPC 测试中的中文类名、方法名和字段名改为符合 Java 命名规范的英文标识符，并执行中文标识符扫描
- [x] 5.4 运行完整测试和构建，重新校验实现与 OpenSpec 文档一致

## 6. 字符串结果载荷

- [x] 6.1 将 `RpcResponse.result` 改为 Jackson JSON 字符串，并同步调整服务端生成和客户端结果转换逻辑
- [x] 6.2 更新公共组件、客户端、服务端和集成测试，覆盖字符串、对象、`null` 及序列化失败结果
- [x] 6.3 运行完整测试、构建和中文标识符扫描，并重新执行 OpenSpec 严格校验

## 7. Netty 异步响应调度

- [x] 7.1 将 `pendingCalls` 的值改为 Netty `Promise<RpcResponse>`，并由独立 `DefaultEventLoop` 调度响应完成与失败
- [x] 7.2 调整同步等待、超时清理和关闭流程，补充异步匹配、事件循环线程保护及资源释放测试
- [x] 7.3 运行完整测试、构建、行宽和中文标识符扫描，并重新执行 OpenSpec 严格校验
