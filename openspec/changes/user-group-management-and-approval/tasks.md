## 1. 数据模型与用户组后端

- [ ] 1.1 新增 Flyway 迁移，创建 `sys_user_group`、`sys_user_group_member`，并为审批关联与动作记录增加节点快照和用户组审计字段
- [ ] 1.2 新增用户组 Entity、Mapper、DTO，并为所有新增类型和方法补齐 JavaDoc
- [ ] 1.3 实现用户组查询、新增、编辑、停用 Service，校验编码唯一、状态和引用边界
- [ ] 1.4 实现成员列表与事务性整体保存，拒绝不存在、非 ACTIVE 或重复用户
- [ ] 1.5 新增薄 Controller 和用户组 REST API，覆盖参数校验、错误响应与接口测试

## 2. 用户组管理前端

- [ ] 2.1 新增用户组 TypeScript 类型及列表、保存、停用、成员维护 API
- [ ] 2.2 新增“用户组管理”导航、路由和页面，支持列表、空状态、加载失败重试
- [ ] 2.3 实现用户组新增/编辑与成员选择交互，保存失败时保留输入内容
- [ ] 2.4 验证窄屏布局、重复编码、停用组和成员整体替换行为

## 3. 设计器与发布校验

- [ ] 3.1 扩展画布节点模型为 `USER|GROUP` 指派，旧节点缺少类型时兼容为 USER
- [ ] 3.2 扩展设计器属性面板，可切换指定用户/用户组并显示组状态和成员数量
- [ ] 3.3 扩展前后端即时/权威校验，拒绝不存在、停用、空成员或指派冲突的用户组节点
- [ ] 3.4 扩展模板发布与版本模型，保存用户组 ID 且不把成员列表固化到模板版本

## 4. 用户组审批运行时

- [ ] 4.1 定义审批域节点指派与实例快照模型，仅保存组 ID 等节点结构，每次查询/提交实时读取最新有效成员
- [ ] 4.2 重构 BPMN 生成器，为 USER 节点生成单人任务，为 GROUP 节点生成关联组 ID 的共享任务
- [ ] 4.3 调整网关活动任务查询和动作处理，按最新成员关系投影共享待办并在提交时重新授权
- [ ] 4.4 实现任一成员同意即完成共享任务并移除其他成员待办、部分驳回继续等待、全部驳回终止流程
- [ ] 4.5 处理成员并发审批冲突，保证只有活动任务可完成且动作记录与流程推进保持事务一致
- [ ] 4.6 实现成员变更与审批结算统一锁顺序，覆盖全部当前成员已驳回的显式结果确认及只读查询
- [ ] 4.7 保持用户新增/编辑 Controller、DTO、页面、业务 Service 调用和审批结果监听器不变

## 5. 审批中心与审计详情

- [ ] 5.1 扩展待办和审批记录 DTO，返回逻辑节点 ID、指派类型及用户组名称
- [ ] 5.2 扩展动作记录持久化，允许共享 taskId 下保存多个实际成员动作，迁移唯一约束为任务与处理人组合
- [ ] 5.3 重构详情聚合为逻辑节点与成员动作结构，正确计算 PENDING、APPROVED、REJECTED、WAITING 和组失效/空组阻塞状态
- [ ] 5.4 更新审批中心与详情弹窗，展示用户组、成员动作、节点最终结果并保持现有用户业务标识展示
- [ ] 5.5 更新详情访问校验，仅允许当前有效候选成员、指定审批人或已有动作记录的用户访问

## 6. 自动化验证与文档

- [ ] 6.1 添加用户组 CRUD、唯一性、停用和成员事务回滚的单元/接口测试
- [ ] 6.2 添加混合 USER/GROUP 画布校验、BPMN 生成和空组发布失败测试
- [ ] 6.3 添加组节点任一同意、部分驳回、全部驳回和并发完成集成测试
- [ ] 6.4 验证运行中新成员获得待办、移出成员提交被拒、清空/停用组保持待处理、恢复后可审批，历史动作不变
- [ ] 6.5 运行后端完整 `gradlew.bat build`、前端 `npm run build` 和 OpenSpec strict validation
- [ ] 6.6 浏览器验证用户组管理、设计器组节点、多人待办刷新、详情审计和响应式布局
- [ ] 6.7 实现完成后同步主规格并归档本 OpenSpec 变更
